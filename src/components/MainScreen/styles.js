import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    flex: 1,
    alignItems: 'center',
    marginTop: 50,
  },
  buttonStyle: {
    borderRadius: 50,
    paddingHorizontal: 20,
  },
});
